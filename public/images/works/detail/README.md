Fallback image directory for works detail sections.

If a matching video file does not exist, image fallback can be used.

Naming rule:
- `<slug>-detail-01.jpg` or `.png`
- `<slug>-detail-02.jpg` or `.png`
